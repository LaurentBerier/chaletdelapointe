You are a senior backend architect building Phase 2 of ChaletDeLaPointe, an Airbnb-style scalable rental platform.

Implement a production-grade Reservation Engine with:

Seasonal pricing
Group-based discounts
Multi-property support
Strict backend validation
RLS enforcement
No Stripe yet (Phase 3)

System must support multiple properties in future but start with Chalet St-Mathieu.

🏗 CORE REQUIREMENTS
1. Multi-Property Ready

All reservations must reference:

property_id
user_id

Never hardcode property logic.

2. Seasonal Pricing Engine

Create seasonal pricing system:

pricing_seasons table
id
property_id
season_name (haute, mi, basse)
start_date
end_date
base_price_per_night
created_at

For Chalet St-Mathieu seed:

Haute saison:

300 CAD
Juillet, août, fériés

Mi-saison:

250 CAD
Juin, septembre, hiver/ski

Basse saison:

175 CAD
Avril–mai, oct–nov

System must determine price dynamically based on date.

3. Group-Based Discount Logic

User groups:

proprietaire
ancien_proprietaire
famille
ami
public (future external renters)

Rules:

Haute saison:

Non membre famille & non VIP: 300
famille + VIP: 50% discount → 150

Implement discount engine:

discount_rules table:

id
property_id
season_name
applicable_group
discount_percentage

Logic:
Final price per night =
base_price - (base_price * discount_percentage)

Must support adding new rules later without rewriting logic.

4. Reservation Lifecycle

reservation_status enum:

draft
pending_payment
confirmed
cancelled
expired

Rules:

User selects dates
Backend validates
System calculates total price
Reservation created as pending_payment
No double booking allowed
Date overlap prevention query required
Admin override possible
5. Price Calculation Engine

Backend function:

calculateReservationPrice(property_id, start_date, end_date, user_group)

Must:

Iterate each night
Determine season
Apply discount if eligible
Return:
nightly breakdown
total
discount applied
season breakdown

No frontend pricing logic.

6. Overlap Prevention

Strict SQL constraint:

Prevent reservation if:

status IN (pending_payment, confirmed)
date range overlaps

Use indexed date fields for performance.

7. Public Safe Calendar View

Create SQL view:

calendar_public

Returns:

property_id
start_date
end_date

Only confirmed reservations.

No user info.
No pricing.
No group info.

8. Admin Override

Admin can:

Force confirm
Cancel reservation
Modify reservation
Bypass seasonal restrictions

All admin actions logged in:

admin_actions table.

9. Performance Requirements

Add indexes on:

property_id
start_date
end_date
status

Must scale to 10+ properties.

10. Deliverables

Generate:

Updated database schema
pricing_seasons table
discount_rules table
Reservation overlap prevention SQL
Price calculation function
Reservation creation server action
Public calendar view
RLS policies
Admin override logic
Test seed data for Chalet St-Mathieu

All code must be production-ready TypeScript + SQL.

No demo shortcuts.

🔒 CRITICAL CONSTRAINTS
All pricing logic backend only
All validation backend only
No price exposed publicly
System must support future:
Multiple properties
Seasonal variations
Promotional codes
Dynamic pricing
Multi-currency